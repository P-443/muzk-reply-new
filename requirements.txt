requests
asyncio==3.4.3
aiofiles==22.1.0
aiohttp==3.8.3
numpy==1.24.1
pillow==9.4.0
psutil
python-telegram-bot==13.7
pyrogram==2.0.97
py-tgcalls==0.9.7
python-dotenv==0.21.1
tgcrypto==1.2.5
speedtest-cli==2.1.3
yt-dlp==2026.08.19
git+https://github.com/alexmercerind/youtube-search-python@main
# youtube-search-python calls httpx.AsyncClient(proxies=...); httpx removed the
# `proxies` kwarg in 0.26, so pin below it or every search crashes with
# "AsyncClient.__init__() got an unexpected keyword argument 'proxies'".
httpx==0.25.2
bgutil-ytdlp-pot-provider==1.3.1
# arabic_reshaper + bidi: required by Helpers/thumbnails.py (the Arabic-title
# template work). Without them the container crashes at module import
# ("ModuleNotFoundError: No module named 'arabic_reshaper'").
arabic-reshaper==2.1.4
python-bidi==0.4.2
